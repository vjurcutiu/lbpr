# ========================
# Environment Configuration
# ========================
ENV=dev  # dev | staging | prod

# ========================
# Firebase
# ========================
FIREBASE_PROJECT_ID=lexbot-pro
FIREBASE_CREDENTIALS=/path/to/firebase-adminsdk.json  # Path to your Firebase credentials
FIREBASE_CREDENTIALS_DEFAULT=/path/to/firebase-adminsdk.json
FIREBASE_STORAGE_BUCKET=lexbot-pro.firebasestorage.app

# ========================
# Session & Cookies
# ========================
COOKIE_NAME=fb_session
SESSION_HOURS=8
COOKIE_SECURE=false       # Set to true in production (HTTPS)
COOKIE_SAMESITE=lax
COOKIE_PATH=/
# COOKIE_DOMAIN=app.example.com  # Uncomment and set in production

# ========================
# CORS / Trusted Hosts
# ========================
# CORS_ALLOW_ORIGINS=["https://app.example.com"]
# TRUSTED_HOSTS=["app.example.com"]

# ========================
# RAG / Embedding Configuration
# ========================
RAG_EMBEDDER=openai
OPENAI_API_KEY=your-openai-api-key-here  # Required for embedding and LLM calls
RAG_EMBED_MODEL=text-embedding-3-small
RAG_VECTORSTORE=pinecone
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_INDEX=lbpr
PINECONE_CLOUD=aws
PINECONE_REGION=us-east-1
RAG_EMBED_DIM=1536

# ========================
# Hybrid Search Settings
# ========================
RAG_HYBRID_FUSION=rrf       # Reciprocal rank fusion
RAG_HYBRID_ALPHA=0.5        # Weight for combining dense/sparse results
RAG_HYBRID_DUAL_INDEX=1     # Whether to use separate dense/sparse indexes
PINECONE_INDEX_DENSE=lbpr-dense
PINECONE_INDEX_SPARSE=lbpr-sparse

# ========================
# Tokenizer
# ========================
TOKENIZER_MODEL=cl100k_base  # Optional: override encoding model

# ========================
# Usage Limits
# ========================
LIMITS_FREE_MESSAGES=50
LIMITS_FREE_UPLOAD_TOKENS=100000
LIMITS_PRO_MESSAGES=10000
LIMITS_PRO_UPLOAD_TOKENS=20000000

# OCR usage caps (number of images/pages processed per period)
LIMITS_FREE_OCR_IMAGES=500
LIMITS_PRO_OCR_IMAGES=200000

# ========================
# Redis
# ========================
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
# REDIS_PASSWORD=your-redis-password


# ========================
# Transcription (Google Speech-to-Text V2)
# ========================
# Regional endpoint / recognizer location. For Chirp 3, "eu" or "us" (multi-region) are common.
STT_LOCATION=eu
STT_MODEL=chirp_3
STT_RECOGNIZER_ID=_
# Comma-separated list used when client does not provide languages
STT_DEFAULT_LANGUAGE_CODES=en-US,cs-CZ,it-IT
# Inline recognize payload size cap (bytes). Larger files should use batch via GCS.
STT_MAX_BYTES=15000000
STT_ENABLE_PUNCTUATION=1
STT_ENABLE_DIARIZATION_DEFAULT=0
STT_DIARIZATION_MIN_SPEAKERS=2
STT_DIARIZATION_MAX_SPEAKERS=6

# Transcription usage caps (billed audio seconds)
LIMITS_FREE_TRANSCRIBE_SECONDS=3600
LIMITS_PRO_TRANSCRIBE_SECONDS=360000

# ========================
# OCR (Google Cloud Vision)
# ========================
OCR_ENABLE=1
OCR_MAX_BYTES=20000000
# Comma-separated BCP-47 language hints (optional). Example: en,cs,it
OCR_DEFAULT_LANGUAGE_HINTS=en,cs,it
# document|text
OCR_MODE=document
# Optional dev fallback (requires system tesseract + pytesseract + pillow)
# OCR_LOCAL_ENABLE=1


